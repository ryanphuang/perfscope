all:
	@echo "Please run \`autoconf/AutoRegen.sh' to generate configure and then run \`configure'."
	@echo "Alternatively, you can invoke the default \`configure' but must provide it with"
	@echo "  --with-llvmsrc and optionally --with-llvmobj each time."
